version: 1
name: Code Quality
description: Run Python quality tools with deterministic settings.
steps:
  - id: ruff
    run: ruff check .
  - id: mypy
    run: mypy src/
  - id: bandit
    run: bandit -q -r src/
  - id: semgrep
    run: semgrep --error --config auto .

